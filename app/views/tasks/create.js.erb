
$("#item").val("");

$('#todo').prepend( $("<%= j render 'task', task: @task %>").hide().fadeIn(500) );


 $todoCount = $("#todo-count-parent"),
 $taskCount = $("#task-count-parent"),



 $taskCount.removeClass('not-displayed')
 $todoCount.removeClass('not-displayed')

 $taskCount.fadeIn()
 $todoCount.fadeIn()

 $todoCount.html('<%= @todo.length %>');
 $taskCount.html('<%= @list.tasks.count %>');
$('[data-toggle="tooltip"]').tooltip();
$('.list--show-header').find( 'span.badge' ).addClass('animation-ring').delay(1500).queue(function(){
    $(this).removeClass('animation-ring').dequeue(); 
  });


